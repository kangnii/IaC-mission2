# Collections Ansible utiles (installer avec : ansible-galaxy install -r requirements.yml)
collections:
  - name: ansible.posix
  - name: community.general
  - name: community.postgresql   # jour 6 : rôle db (création base/utilisateur/table)
