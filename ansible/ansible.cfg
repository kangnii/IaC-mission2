[defaults]
inventory = inventory/hosts.ini
host_key_checking = False
roles_path = roles
retry_files_enabled = False
stdout_callback = yaml
interpreter_python = auto_silent

[ssh_connection]
# Rebond par le bastion pour joindre web et db (réseau privé)
pipelining = True
