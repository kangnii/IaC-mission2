---
# Jour 6 : la version PHP, les chemins TLS et le CN du certificat sont
# définis dans ansible/roles/web/defaults/main.yml (pas de nom de domaine
# réel en démo locale, le CN utilise l'IP privée). Ce fichier reste
# disponible pour surcharger ces valeurs par environnement si besoin.
